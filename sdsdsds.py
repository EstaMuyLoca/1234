#показывает какие обьекты хранятся на раб столе. в телеге есть код что смотреть во всех папках
import os
from pathlib import Path

# Получаем путь к рабочему столу пользователя
desktop_path = Path.home() / "Desktop"

print("Объекты на рабочем столе:")
print("-" * 40)

# Выводим все файлы и папки
for item in sorted(desktop_path.iterdir()):
    if item.is_dir():
        print(f"📁 Папка: {item.name}")
    elif item.is_file():
        print(f"📄 Файл: {item.name}")
    else:
        print(f"🔗 {item.name}")